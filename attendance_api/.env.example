##
## Attendance System (PHP API) environment variables
##
## Copy to `attendance_api/.env` and fill values for your machine.
##

# Database
DB_HOST=localhost
DB_NAME=attendance_db
DB_USER=root
DB_PASS=CHANGE_ME
DB_CHARSET=utf8mb4

# JWT
# Change this for any real deployment.
# HS256 requires >= 32 bytes of key material.
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_STRING_AT_LEAST_32_CHARS

# Admin API protection (recommended)
ADMIN_AUTH_REQUIRED=true

# Optional: CORS (comma-separated list). For local dev you can keep "*".
# CORS_ALLOWED_ORIGINS=*
